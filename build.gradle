apply plugin: 'idea'
apply plugin: 'groovy'

repositories {
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile(
            'com.twitter:scrooge-generator:3.0.4'
    )
    groovy localGroovy()
    testCompile(
            'org.spockframework:spock-core:0.7-groovy-1.8'
    )
}